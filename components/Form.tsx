import { Container } from "@mantine/core";
import { Input } from "./Organisms/Input";

const Form = () => {
    return (
        <Container size="lg">
            <Input />
        </Container>
    );
}

export default Form;